# Discord Bot Token（必填）
# 从 https://discord.com/developers/applications 获取
# ⚠️ 重置后的 token 填在这里，不要填已经泄露的旧 token！
DISCORD_BOT_TOKEN=你的Bot_Token填在这里

# 领取单蓝链的公网根地址（不要末尾斜杠）
# bot-hosting.net：面板「域名」页的规范网址，例如
# PUBLIC_BASE_URL=https://v4fbz80a0x.apps.bot-hosting.cloud
PUBLIC_BASE_URL=

# bot-hosting.net 面板写的应用程序端口（你这边是 25243）
# 不填则优先用 SERVER_PORT，再退回 PORT，最后 8080
# PORT=25243
#
# 贝多芬点唱机语音房：频道名含「音乐世家」
# 托管环境需要 ffmpeg，YouTube / SoundCloud 链接靠 yt-dlp 解析后在语音房播放
#
# B站 解析走官方接口（view 拿分区判音乐，playurl 拿纯音频轨）
#
# ⚠️ 托管机房 IP 直连 B站 很容易被风控（412 / 返回 HTML 页），表现为「链接解析失败」。
# 配一个登录后的 Cookie 就能稳定。获取办法：
#   1. 浏览器登录 bilibili.com
#   2. F12 → Application（存储）→ Cookies → https://www.bilibili.com
#   3. 复制 SESSDATA 和 buvid3 两行的值，拼成下面这种格式
# BILI_COOKIE=SESSDATA=你的SESSDATA; buvid3=你的buvid3
#
# 只有 YouTube / SoundCloud 需要登录态时才要配这个 cookies 文件（Netscape 格式）
# YTDLP_COOKIEFILE=/app/cookies.txt